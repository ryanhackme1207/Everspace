{% load static %}{% load static %}<!DOCTYPE html>

<!DOCTYPE html>

<html lang="en"><!DOCTYPE html><html lang="en">

<head>

    <meta charset="UTF-8"><html lang="en"><head>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>EverSpace - Where Conversations Come Alive</title><head>    <meta charset="UTF-8">

    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">

    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">    <meta charset="UTF-8">    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <style>

        * {    <meta name="viewport" content="width=device-width, initial-scale=1.0">    <title>EverSpace - Next Generation Chat Experience</title>

            margin: 0;

            padding: 0;    <title>EverSpace - Where Conversations Come Alive</title>    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">

            box-sizing: border-box;

        }    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">



        body {    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">    <style>

            font-family: 'Inter', sans-serif;

            background: #0d1117;    <style>        * {

            color: #c9d1d9;

            overflow-x: hidden;        * {            margin: 0;

        }

            margin: 0;            padding: 0;

        /* Navigation */

        nav {            padding: 0;            box-sizing: border-box;

            position: fixed;

            top: 0;            box-sizing: border-box;        }

            width: 100%;

            background: rgba(13, 17, 23, 0.8);        }

            backdrop-filter: blur(20px);

            border-bottom: 1px solid rgba(255, 255, 255, 0.1);        :root {

            z-index: 1000;

            padding: 1rem 0;        body {            /* Modern Ocean & Sunset Theme */

        }

            font-family: 'Inter', sans-serif;            --primary-gradient: linear-gradient(135deg, #FF6B6B 0%, #4ECDC4 50%, #45B7D1 100%);

        .nav-container {

            max-width: 1200px;            background: #0d1117;            --secondary-gradient: linear-gradient(135deg, #96CEB4 0%, #FECA57 100%);

            margin: 0 auto;

            padding: 0 2rem;            color: #c9d1d9;            --success-gradient: linear-gradient(135deg, #00C9FF 0%, #92FE9D 100%);

            display: flex;

            justify-content: space-between;            overflow-x: hidden;            --danger-gradient: linear-gradient(135deg, #FC466B 0%, #3F5EFB 100%);

            align-items: center;

        }        }            --warning-gradient: linear-gradient(135deg, #FDBB2D 0%, #22C1C3 100%);



        .logo {            --glass-bg: rgba(20, 25, 40, 0.3);

            font-size: 1.5rem;

            font-weight: 800;        /* Navigation */            --glass-border: rgba(255, 255, 255, 0.15);

            background: linear-gradient(135deg, #ff6b6b, #4ecdc4);

            -webkit-background-clip: text;        nav {            --dark-bg: #0F1419;

            -webkit-text-fill-color: transparent;

            background-clip: text;            position: fixed;            --dark-card: #1A202C;

        }

            top: 0;            --darker-card: #2D3748;

        .nav-links {

            display: flex;            width: 100%;            --text-primary: #F7FAFC;

            gap: 2rem;

            align-items: center;            background: rgba(13, 17, 23, 0.8);            --text-secondary: #A0AEC0;

        }

            backdrop-filter: blur(20px);            --text-muted: #718096;

        .nav-links a {

            color: #c9d1d9;            border-bottom: 1px solid rgba(255, 255, 255, 0.1);            --accent-coral: #FF6B6B;

            text-decoration: none;

            font-weight: 500;            z-index: 1000;            --accent-teal: #4ECDC4;

            transition: color 0.3s;

        }            padding: 1rem 0;            --accent-blue: #45B7D1;



        .nav-links a:hover {        }            --accent-mint: #96CEB4;

            color: #4ecdc4;

        }            --accent-yellow: #FECA57;



        .btn {        .nav-container {            --border-radius-lg: 24px;

            padding: 0.75rem 1.5rem;

            border-radius: 8px;            max-width: 1200px;            --border-radius-md: 16px;

            font-weight: 600;

            text-decoration: none;            margin: 0 auto;            --border-radius-sm: 12px;

            transition: all 0.3s;

            display: inline-block;            padding: 0 2rem;            --shadow-glass: 0 8px 32px rgba(15, 20, 25, 0.4);

        }

            display: flex;            --shadow-elevated: 0 20px 40px rgba(15, 20, 25, 0.6);

        .btn-primary {

            background: linear-gradient(135deg, #4ecdc4, #45b7d1);            justify-content: space-between;        }

            color: white;

            border: none;            align-items: center;

        }

        }        body {

        .btn-primary:hover {

            transform: translateY(-2px);            font-family: 'Inter', sans-serif;

            box-shadow: 0 10px 30px rgba(78, 205, 196, 0.3);

        }        .logo {            background: linear-gradient(135deg, #0F1419 0%, #1A202C 30%, #2D3748 70%, #4A5568 100%);



        .btn-secondary {            font-size: 1.5rem;            background-attachment: fixed;

            background: transparent;

            color: white;            font-weight: 800;            color: var(--text-primary);

            border: 2px solid rgba(255, 255, 255, 0.2);

        }            background: linear-gradient(135deg, #ff6b6b, #4ecdc4);            overflow-x: hidden;



        .btn-secondary:hover {            -webkit-background-clip: text;            line-height: 1.6;

            border-color: #4ecdc4;

            color: #4ecdc4;            -webkit-text-fill-color: transparent;        }

        }

            background-clip: text;

        /* Hero Section */

        .hero {        }        /* Animated Background */

            min-height: 100vh;

            display: flex;        .bg-animation {

            align-items: center;

            justify-content: center;        .nav-links {            position: fixed;

            position: relative;

            padding: 6rem 2rem 2rem;            display: flex;            top: 0;

            overflow: hidden;

        }            gap: 2rem;            left: 0;



        .hero::before {            align-items: center;            width: 100%;

            content: '';

            position: absolute;        }            height: 100%;

            width: 150%;

            height: 150%;            z-index: -1;

            background: radial-gradient(circle at 50% 50%, rgba(78, 205, 196, 0.1) 0%, transparent 50%);

            animation: pulse 8s ease-in-out infinite;        .nav-links a {            background: linear-gradient(-45deg, #FF6B6B, #4ECDC4, #45B7D1, #96CEB4, #FECA57);

        }

            color: #c9d1d9;            background-size: 400% 400%;

        @keyframes pulse {

            0%, 100% { transform: scale(1); opacity: 0.5; }            text-decoration: none;            animation: gradientShift 15s ease infinite;

            50% { transform: scale(1.2); opacity: 0.8; }

        }            font-weight: 500;            opacity: 0.1;



        .hero-content {            transition: color 0.3s;        }

            max-width: 1200px;

            text-align: center;        }

            position: relative;

            z-index: 1;        @keyframes gradientShift {

        }

        .nav-links a:hover {            0% { background-position: 0% 50%; }

        h1 {

            font-size: 4rem;            color: #4ecdc4;            50% { background-position: 100% 50%; }

            font-weight: 900;

            margin-bottom: 1.5rem;        }            100% { background-position: 0% 50%; }

            line-height: 1.1;

        }        }



        .gradient-text {        .btn {

            background: linear-gradient(135deg, #ff6b6b, #4ecdc4, #45b7d1);

            -webkit-background-clip: text;            padding: 0.75rem 1.5rem;        /* Floating Particles */

            -webkit-text-fill-color: transparent;

            background-clip: text;            border-radius: 8px;        .particles {

            animation: gradientShift 3s ease infinite;

            background-size: 200% 200%;            font-weight: 600;            position: fixed;

        }

            text-decoration: none;            top: 0;

        @keyframes gradientShift {

            0%, 100% { background-position: 0% 50%; }            transition: all 0.3s;            left: 0;

            50% { background-position: 100% 50%; }

        }            display: inline-block;            width: 100%;



        .subtitle {        }            height: 100%;

            font-size: 1.5rem;

            color: #8b949e;            pointer-events: none;

            margin-bottom: 3rem;

            max-width: 800px;        .btn-primary {            z-index: -1;

            margin-left: auto;

            margin-right: auto;            background: linear-gradient(135deg, #4ecdc4, #45b7d1);        }

        }

            color: white;

        .cta-buttons {

            display: flex;            border: none;        .particle {

            gap: 1.5rem;

            justify-content: center;        }            position: absolute;

            flex-wrap: wrap;

        }            background: linear-gradient(45deg, var(--accent-coral), var(--accent-teal));



        .cta-buttons .btn {        .btn-primary:hover {            border-radius: 50%;

            font-size: 1.1rem;

            padding: 1rem 2rem;            transform: translateY(-2px);            animation: float 20s infinite linear;

        }

            box-shadow: 0 10px 30px rgba(78, 205, 196, 0.3);            box-shadow: 0 0 15px rgba(255, 107, 107, 0.3);

        /* Features Section */

        .features {        }        }

            padding: 6rem 2rem;

            background: linear-gradient(180deg, #0d1117 0%, #161b22 50%, #0d1117 100%);

        }

        .btn-secondary {        @keyframes float {

        .section-title {

            text-align: center;            background: transparent;            0% {

            font-size: 3rem;

            font-weight: 800;            color: white;                transform: translateY(100vh) rotate(0deg);

            margin-bottom: 4rem;

        }            border: 2px solid rgba(255, 255, 255, 0.2);                opacity: 0;



        .features-grid {        }            }

            max-width: 1200px;

            margin: 0 auto;            10% {

            display: grid;

            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));        .btn-secondary:hover {                opacity: 1;

            gap: 2rem;

        }            border-color: #4ecdc4;            }



        .feature-card {            color: #4ecdc4;            90% {

            background: rgba(22, 27, 34, 0.8);

            border: 1px solid rgba(255, 255, 255, 0.1);        }                opacity: 1;

            border-radius: 16px;

            padding: 2rem;            }

            transition: all 0.3s;

            backdrop-filter: blur(10px);        /* Hero Section */            100% {

        }

        .hero {                transform: translateY(-100px) rotate(360deg);

        .feature-card:hover {

            transform: translateY(-5px);            min-height: 100vh;                opacity: 0;

            border-color: #4ecdc4;

            box-shadow: 0 20px 40px rgba(78, 205, 196, 0.2);            display: flex;            }

        }

            align-items: center;        }

        .feature-icon {

            width: 64px;            justify-content: center;

            height: 64px;

            background: linear-gradient(135deg, #ff6b6b, #4ecdc4);            position: relative;        /* Header */

            border-radius: 12px;

            display: flex;            padding: 6rem 2rem 2rem;        .header {

            align-items: center;

            justify-content: center;            overflow: hidden;            position: fixed;

            font-size: 2rem;

            margin-bottom: 1.5rem;        }            top: 0;

        }

            left: 0;

        .feature-card h3 {

            font-size: 1.5rem;        .hero::before {            right: 0;

            font-weight: 700;

            margin-bottom: 1rem;            content: '';            z-index: 1000;

        }

            position: absolute;            padding: 1rem 2rem;

        .feature-card p {

            color: #8b949e;            width: 150%;            background: var(--dark-card);

            line-height: 1.6;

        }            height: 150%;            backdrop-filter: blur(20px);



        /* Stats Section */            background: radial-gradient(circle at 50% 50%, rgba(78, 205, 196, 0.1) 0%, transparent 50%);            border-bottom: 1px solid var(--accent-teal);

        .stats {

            padding: 6rem 2rem;            animation: pulse 8s ease-in-out infinite;            box-shadow: var(--shadow-glass);

            background: #0d1117;

        }        }            border-bottom: 1px solid rgba(255, 255, 255, 0.1);



        .stats-grid {            transition: all 0.3s ease;

            max-width: 1200px;

            margin: 0 auto;        @keyframes pulse {        }

            display: grid;

            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));            0%, 100% { transform: scale(1); opacity: 0.5; }

            gap: 3rem;

            text-align: center;            50% { transform: scale(1.2); opacity: 0.8; }        .nav {

        }

        }            display: flex;

        .stat-item {

            padding: 2rem;            justify-content: space-between;

        }

        .hero-content {            align-items: center;

        .stat-number {

            font-size: 3.5rem;            max-width: 1200px;            max-width: 1200px;

            font-weight: 900;

            background: linear-gradient(135deg, #ff6b6b, #4ecdc4);            text-align: center;            margin: 0 auto;

            -webkit-background-clip: text;

            -webkit-text-fill-color: transparent;            position: relative;        }

            background-clip: text;

            margin-bottom: 0.5rem;            z-index: 1;

        }

        }        .logo {

        .stat-label {

            font-size: 1.1rem;            font-size: 1.8rem;

            color: #8b949e;

        }        h1 {            font-weight: 800;



        /* Testimonials */            font-size: 4rem;            background: var(--primary-gradient);

        .testimonials {

            padding: 6rem 2rem;            font-weight: 900;            -webkit-background-clip: text;

            background: linear-gradient(180deg, #0d1117 0%, #161b22 100%);

        }            margin-bottom: 1.5rem;            -webkit-text-fill-color: transparent;



        .testimonials-grid {            line-height: 1.1;            background-clip: text;

            max-width: 1200px;

            margin: 0 auto;        }            animation: logoGlow 3s ease-in-out infinite alternate;

            display: grid;

            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));        }

            gap: 2rem;

        }        .gradient-text {



        .testimonial-card {            background: linear-gradient(135deg, #ff6b6b, #4ecdc4, #45b7d1);        @keyframes logoGlow {

            background: rgba(22, 27, 34, 0.8);

            border: 1px solid rgba(255, 255, 255, 0.1);            -webkit-background-clip: text;            0% { filter: drop-shadow(0 0 5px rgba(255, 107, 107, 0.5)); }

            border-radius: 16px;

            padding: 2rem;            -webkit-text-fill-color: transparent;            100% { filter: drop-shadow(0 0 20px rgba(255, 107, 107, 0.8)); }

            backdrop-filter: blur(10px);

        }            background-clip: text;        }



        .testimonial-text {            animation: gradientShift 3s ease infinite;

            font-size: 1.1rem;

            line-height: 1.8;            background-size: 200% 200%;        .nav-buttons {

            margin-bottom: 1.5rem;

            color: #c9d1d9;        }            display: flex;

        }

            gap: 1rem;

        .testimonial-author {

            display: flex;        @keyframes gradientShift {        }

            align-items: center;

            gap: 1rem;            0%, 100% { background-position: 0% 50%; }

        }

            50% { background-position: 100% 50%; }        .btn {

        .author-avatar {

            width: 50px;        }            padding: 0.75rem 1.5rem;

            height: 50px;

            background: linear-gradient(135deg, #ff6b6b, #4ecdc4);            border: none;

            border-radius: 50%;

            display: flex;        .subtitle {            border-radius: 50px;

            align-items: center;

            justify-content: center;            font-size: 1.5rem;            font-weight: 600;

            font-weight: 700;

            font-size: 1.2rem;            color: #8b949e;            text-decoration: none;

        }

            margin-bottom: 3rem;            transition: all 0.3s ease;

        .author-info h4 {

            font-size: 1rem;            max-width: 800px;            cursor: pointer;

            font-weight: 600;

            margin-bottom: 0.25rem;            margin-left: auto;            position: relative;

        }

            margin-right: auto;            overflow: hidden;

        .author-info p {

            font-size: 0.9rem;        }        }

            color: #8b949e;

        }



        /* Footer */        .cta-buttons {        .btn-primary {

        footer {

            padding: 4rem 2rem;            display: flex;            background: var(--primary-gradient);

            background: #0d1117;

            border-top: 1px solid rgba(255, 255, 255, 0.1);            gap: 1.5rem;            color: var(--text-primary);

        }

            justify-content: center;            box-shadow: 0 4px 15px rgba(255, 107, 107, 0.4);

        .footer-content {

            max-width: 1200px;            flex-wrap: wrap;        }

            margin: 0 auto;

            text-align: center;        }

        }

        .btn-secondary {

        .footer-logo {

            font-size: 2rem;        .cta-buttons .btn {            background: var(--dark-card);

            font-weight: 800;

            margin-bottom: 1rem;            font-size: 1.1rem;            color: var(--accent-teal);

        }

            padding: 1rem 2rem;            border: 2px solid var(--accent-teal);

        .footer-links {

            display: flex;        }        }

            gap: 2rem;

            justify-content: center;

            margin: 2rem 0;

            flex-wrap: wrap;        /* Features Section */        .btn:hover {

        }

        .features {            transform: translateY(-2px);

        .footer-links a {

            color: #8b949e;            padding: 6rem 2rem;            box-shadow: 0 8px 25px rgba(255, 107, 107, 0.6);

            text-decoration: none;

            transition: color 0.3s;            background: linear-gradient(180deg, #0d1117 0%, #161b22 50%, #0d1117 100%);        }

        }

        }

        .footer-links a:hover {

            color: #4ecdc4;        .btn-secondary:hover {

        }

        .section-title {            background: var(--accent-teal);

        .copyright {

            color: #8b949e;            text-align: center;            color: var(--dark-bg);

            margin-top: 2rem;

        }            font-size: 3rem;            box-shadow: 0 8px 25px rgba(78, 205, 196, 0.4);



        /* Responsive */            font-weight: 800;        }

        @media (max-width: 768px) {

            h1 {            margin-bottom: 4rem;

                font-size: 2.5rem;

            }        }        .btn::before {



            .subtitle {            content: '';

                font-size: 1.2rem;

            }        .features-grid {            position: absolute;



            .section-title {            max-width: 1200px;            top: 0;

                font-size: 2rem;

            }            margin: 0 auto;            left: -100%;



            .features-grid,            display: grid;            width: 100%;

            .testimonials-grid {

                grid-template-columns: 1fr;            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));            height: 100%;

            }

            gap: 2rem;            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);

            .nav-links {

                display: none;        }            transition: left 0.5s;

            }

        }        }

    </style>

</head>        .feature-card {

<body>

    <!-- Navigation -->            background: rgba(22, 27, 34, 0.8);        .btn:hover::before {

    <nav>

        <div class="nav-container">            border: 1px solid rgba(255, 255, 255, 0.1);            left: 100%;

            <div class="logo">🚀 EverSpace</div>

            <div class="nav-links">            border-radius: 16px;        }

                <a href="{% url 'chat:landing' %}">Home</a>

                <a href="{% url 'chat:about' %}">About</a>            padding: 2rem;

                <a href="#features">Features</a>

                <a href="#stats">Stats</a>            transition: all 0.3s;        /* Hero Section */

                <a href="#testimonials">Testimonials</a>

                {% if user.is_authenticated %}            backdrop-filter: blur(10px);        .hero {

                    <a href="{% url 'chat:index' %}" class="btn btn-primary">Go to Chat</a>

                {% else %}        }            min-height: 100vh;

                    <a href="{% url 'authentication:login' %}" class="btn btn-secondary">Login</a>

                    <a href="{% url 'authentication:register' %}" class="btn btn-primary">Get Started</a>            display: flex;

                {% endif %}

            </div>        .feature-card:hover {            align-items: center;

        </div>

    </nav>            transform: translateY(-5px);            justify-content: center;



    <!-- Hero Section -->            border-color: #4ecdc4;            text-align: center;

    <section class="hero">

        <div class="hero-content">            box-shadow: 0 20px 40px rgba(78, 205, 196, 0.2);            padding: 2rem;

            <h1><span class="gradient-text">EverSpace</span></h1>

            <p class="subtitle">Where Conversations Come Alive</p>        }            position: relative;

            <p style="font-size: 1.2rem; color: #8b949e; margin-bottom: 3rem; max-width: 900px; margin-left: auto; margin-right: auto;">

                Experience the next generation of secure, real-time communication. With military-grade security, lightning-fast messaging, and stunning visual design, EverSpace transforms how you connect with others. Join a community that values privacy, speed, and beautiful design.        }

            </p>

            <div class="cta-buttons">        .feature-icon {

                {% if user.is_authenticated %}

                    <a href="{% url 'chat:index' %}" class="btn btn-primary">            width: 64px;        .hero-content {

                        <i class="fas fa-rocket"></i> Launch Into Chat

                    </a>            height: 64px;            max-width: 800px;

                {% else %}

                    <a href="{% url 'authentication:register' %}" class="btn btn-primary">            background: linear-gradient(135deg, #ff6b6b, #4ecdc4);            z-index: 10;

                        <i class="fas fa-rocket"></i> Launch Into Chat

                    </a>            border-radius: 12px;        }

                {% endif %}

                <a href="#features" class="btn btn-secondary">            display: flex;

                    <i class="fas fa-compass"></i> Explore Features

                </a>            align-items: center;        .hero-title {

            </div>

        </div>            justify-content: center;            font-size: clamp(3rem, 8vw, 6rem);

    </section>

            font-size: 2rem;            font-weight: 800;

    <!-- Features Section -->

    <section class="features" id="features">            margin-bottom: 1.5rem;            margin-bottom: 1rem;

        <h2 class="section-title">Revolutionary Features</h2>

        <div class="features-grid">        }            background: var(--primary-gradient);

            <div class="feature-card">

                <div class="feature-icon">            -webkit-background-clip: text;

                    <i class="fas fa-shield-halved"></i>

                </div>        .feature-card h3 {            -webkit-text-fill-color: transparent;

                <h3>Military-Grade Security</h3>

                <p>Multi-factor authentication, end-to-end encryption, and advanced threat protection keep your conversations absolutely secure.</p>            font-size: 1.5rem;            background-clip: text;

            </div>

            font-weight: 700;            animation: titleFloat 6s ease-in-out infinite;

            <div class="feature-card">

                <div class="feature-icon">            margin-bottom: 1rem;            text-shadow: 0 0 50px rgba(255, 107, 107, 0.3);

                    <i class="fas fa-bolt"></i>

                </div>        }        }

                <h3>Lightning Fast</h3>

                <p>Real-time messaging with WebSocket technology ensures your messages arrive instantly with zero lag.</p>

            </div>

        .feature-card p {        @keyframes titleFloat {

            <div class="feature-card">

                <div class="feature-icon">            color: #8b949e;            0%, 100% { transform: translateY(0px); }

                    <i class="fas fa-sparkles"></i>

                </div>            line-height: 1.6;            50% { transform: translateY(-10px); }

                <h3>Immersive Experience</h3>

                <p>Beautiful animations, smooth transitions, and a modern interface that makes chatting a delightful experience.</p>        }        }

            </div>



            <div class="feature-card">

                <div class="feature-icon">        /* Stats Section */        .hero-subtitle {

                    <i class="fas fa-layer-group"></i>

                </div>        .stats {            font-size: 1.3rem;

                <h3>Multiple Rooms</h3>

                <p>Create or join unlimited chat rooms. Organize conversations by topics, teams, or interests.</p>            padding: 6rem 2rem;            margin-bottom: 2rem;

            </div>

            background: #0d1117;            opacity: 1;

            <div class="feature-card">

                <div class="feature-icon">        }            animation: fadeInUp 1s ease 0.5s both;

                    <i class="fas fa-mobile-screen"></i>

                </div>            color: var(--text-secondary);

                <h3>Cross-Platform</h3>

                <p>Fully responsive design works seamlessly on desktop, tablet, and mobile devices. Chat anywhere, anytime.</p>        .stats-grid {            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);

            </div>

            max-width: 1200px;        }

            <div class="feature-card">

                <div class="feature-icon">            margin: 0 auto;

                    <i class="fas fa-code"></i>

                </div>            display: grid;        .hero-description {

                <h3>Modern Technology</h3>

                <p>Built with cutting-edge web technologies including Django Channels, WebSockets, and modern JavaScript.</p>            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));            font-size: 1.1rem;

            </div>

        </div>            gap: 3rem;            margin-bottom: 3rem;

    </section>

            text-align: center;            opacity: 0.95;

    <!-- Stats Section -->

    <section class="stats" id="stats">        }            max-width: 600px;

        <div class="stats-grid">

            <div class="stat-item">            margin-left: auto;

                <div class="stat-number">100<small style="font-size: 0.6em;">%</small></div>

                <div class="stat-label">Uptime</div>        .stat-item {            margin-right: auto;

            </div>

            <div class="stat-item">            padding: 2rem;            animation: fadeInUp 1s ease 0.7s both;

                <div class="stat-number">256</div>

                <div class="stat-label">Bit Encryption</div>        }            color: var(--text-primary);

            </div>

            <div class="stat-item">            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.3);

                <div class="stat-number">0.001<small style="font-size: 0.6em;">s</small></div>

                <div class="stat-label">Second Latency</div>        .stat-number {            line-height: 1.6;

            </div>

            <div class="stat-item">            font-size: 3.5rem;        }

                <div class="stat-number">24<small style="font-size: 0.6em;">/7</small></div>

                <div class="stat-label">Support</div>            font-weight: 900;

            </div>

        </div>            background: linear-gradient(135deg, #ff6b6b, #4ecdc4);        @keyframes fadeInUp {

    </section>

            -webkit-background-clip: text;            from {

    <!-- Testimonials Section -->

    <section class="testimonials" id="testimonials">            -webkit-text-fill-color: transparent;                opacity: 0;

        <h2 class="section-title">What Our Users Say</h2>

        <div class="testimonials-grid">            background-clip: text;                transform: translateY(30px);

            <div class="testimonial-card">

                <p class="testimonial-text">"EverSpace has revolutionized how our team communicates. The security features give us peace of mind, and the interface is absolutely beautiful!"</p>            margin-bottom: 0.5rem;            }

                <div class="testimonial-author">

                    <div class="author-avatar">SJ</div>        }            to {

                    <div class="author-info">

                        <h4>Sarah Johnson</h4>                opacity: 1;

                        <p>Tech Lead, InnovateCorp</p>

                    </div>        .stat-label {                transform: translateY(0);

                </div>

            </div>            font-size: 1.1rem;            }



            <div class="testimonial-card">            color: #8b949e;        }

                <p class="testimonial-text">"The real-time messaging is incredibly fast, and I love the modern design. It makes chatting feel like a premium experience."</p>

                <div class="testimonial-author">        }

                    <div class="author-avatar">AC</div>

                    <div class="author-info">        .hero-cta {

                        <h4>Alex Chen</h4>

                        <p>Software Engineer</p>        /* Testimonials */            display: flex;

                    </div>

                </div>        .testimonials {            gap: 1rem;

            </div>

            padding: 6rem 2rem;            justify-content: center;

            <div class="testimonial-card">

                <p class="testimonial-text">"Multi-factor authentication and room management features are exactly what we needed for our organization. Highly recommended!"</p>            background: linear-gradient(180deg, #0d1117 0%, #161b22 100%);            flex-wrap: wrap;

                <div class="testimonial-author">

                    <div class="author-avatar">MP</div>        }            animation: fadeInUp 1s ease 0.9s both;

                    <div class="author-info">

                        <h4>Michael Park</h4>        }

                        <p>Operations Manager</p>

                    </div>        .testimonials-grid {

                </div>

            </div>            max-width: 1200px;        .btn-hero {

        </div>

    </section>            margin: 0 auto;            padding: 1rem 2rem;



    <!-- Footer -->            display: grid;            font-size: 1.1rem;

    <footer>

        <div class="footer-content">            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));            border-radius: 50px;

            <div class="footer-logo gradient-text">🚀 EverSpace</div>

            <div class="footer-links">            gap: 2rem;            position: relative;

                <a href="{% url 'chat:landing' %}">Home</a>

                <a href="{% url 'chat:about' %}">About</a>        }            overflow: hidden;

                <a href="#features">Features</a>

                <a href="#stats">Stats</a>        }

                <a href="#testimonials">Testimonials</a>

                {% if not user.is_authenticated %}        .testimonial-card {

                    <a href="{% url 'authentication:login' %}">Login</a>

                    <a href="{% url 'authentication:register' %}">Register</a>            background: rgba(22, 27, 34, 0.8);        .btn-hero-primary {

                {% endif %}

            </div>            border: 1px solid rgba(255, 255, 255, 0.1);            background: var(--primary-gradient);

            <p class="copyright">© 2025 EverSpace. Created by Ryan Lai. All rights reserved.</p>

        </div>            border-radius: 16px;            color: var(--text-primary);

    </footer>

</body>            padding: 2rem;            box-shadow: 0 8px 30px rgba(255, 107, 107, 0.4);

</html>

            backdrop-filter: blur(10px);        }

        }

        .btn-hero-secondary {

        .testimonial-text {            background: var(--dark-card);

            font-size: 1.1rem;            color: var(--accent-teal);

            line-height: 1.8;            border: 2px solid var(--accent-teal);

            margin-bottom: 1.5rem;            backdrop-filter: blur(10px);

            color: #c9d1d9;        }

        }

        .btn-hero-secondary:hover {

        .testimonial-author {            background: var(--accent-teal);

            display: flex;            color: var(--dark-bg);

            align-items: center;            box-shadow: 0 8px 30px rgba(78, 205, 196, 0.4);

            gap: 1rem;        }

        }

        /* Features Section */

        .author-avatar {        .features {

            width: 50px;            padding: 8rem 2rem;

            height: 50px;            max-width: 1200px;

            background: linear-gradient(135deg, #ff6b6b, #4ecdc4);            margin: 0 auto;

            border-radius: 50%;        }

            display: flex;

            align-items: center;        .features-title {

            justify-content: center;            text-align: center;

            font-weight: 700;            font-size: 3rem;

            font-size: 1.2rem;            font-weight: 700;

        }            margin-bottom: 4rem;

            background: var(--secondary-gradient);

        .author-info h4 {            -webkit-background-clip: text;

            font-size: 1rem;            -webkit-text-fill-color: transparent;

            font-weight: 600;            background-clip: text;

            margin-bottom: 0.25rem;        }

        }

        .features-grid {

        .author-info p {            display: grid;

            font-size: 0.9rem;            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));

            color: #8b949e;            gap: 2rem;

        }        }



        /* Footer */        .feature-card {

        footer {            background: var(--dark-card);

            padding: 4rem 2rem;            backdrop-filter: blur(20px);

            background: #0d1117;            padding: 2rem;

            border-top: 1px solid rgba(255, 255, 255, 0.1);            border-radius: var(--border-radius-lg);

        }            border: 1px solid var(--glass-border);

            transition: all 0.3s ease;

        .footer-content {            position: relative;

            max-width: 1200px;            overflow: hidden;

            margin: 0 auto;            box-shadow: var(--shadow-glass);

            text-align: center;        }

        }

        .feature-card::before {

        .footer-logo {            content: '';

            font-size: 2rem;            position: absolute;

            font-weight: 800;            top: 0;

            margin-bottom: 1rem;            left: 0;

        }            right: 0;

            height: 3px;

        .footer-links {            background: var(--primary-gradient);

            display: flex;            transform: scaleX(0);

            gap: 2rem;            transition: transform 0.3s ease;

            justify-content: center;        }

            margin: 2rem 0;

            flex-wrap: wrap;        .feature-card:hover::before {

        }            transform: scaleX(1);

        }

        .footer-links a {

            color: #8b949e;        .feature-card:hover {

            text-decoration: none;            transform: translateY(-10px);

            transition: color 0.3s;            box-shadow: var(--shadow-elevated);

        }            border-color: var(--accent-teal);

        }

        .footer-links a:hover {

            color: #4ecdc4;        .feature-icon {

        }            font-size: 3rem;

            margin-bottom: 1rem;

        .copyright {            background: var(--primary-gradient);

            color: #8b949e;            -webkit-background-clip: text;

            margin-top: 2rem;            -webkit-text-fill-color: transparent;

        }            background-clip: text;

        }

        /* Responsive */

        @media (max-width: 768px) {        .feature-title {

            h1 {            font-size: 1.3rem;

                font-size: 2.5rem;            font-weight: 600;

            }            margin-bottom: 1rem;

            color: var(--text-primary);

            .subtitle {        }

                font-size: 1.2rem;

            }        .feature-description {

            opacity: 0.95;

            .section-title {            line-height: 1.6;

                font-size: 2rem;            color: var(--text-secondary);

            }        }



            .features-grid,        /* Stats Section */

            .testimonials-grid {        .stats {

                grid-template-columns: 1fr;            padding: 4rem 2rem;

            }            text-align: center;

            background: rgba(0, 0, 0, 0.2);

            .nav-links {        }

                display: none;

            }        .stats-grid {

        }            display: grid;

    </style>            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));

</head>            gap: 2rem;

<body>            max-width: 800px;

    <!-- Navigation -->            margin: 0 auto;

    <nav>        }

        <div class="nav-container">

            <div class="logo">🚀 EverSpace</div>        .stat-item {

            <div class="nav-links">            padding: 1rem;

                <a href="#features">Features</a>        }

                <a href="#stats">Stats</a>

                <a href="#testimonials">Testimonials</a>        .stat-number {

                {% if user.is_authenticated %}            font-size: 3rem;

                    <a href="{% url 'chat:index' %}" class="btn btn-primary">Go to Chat</a>            font-weight: 800;

                {% else %}            background: var(--success-gradient);

                    <a href="{% url 'authentication:login' %}" class="btn btn-secondary">Login</a>            -webkit-background-clip: text;

                    <a href="{% url 'authentication:register' %}" class="btn btn-primary">Get Started</a>            -webkit-text-fill-color: transparent;

                {% endif %}            background-clip: text;

            </div>            margin-bottom: 0.5rem;

        </div>        }

    </nav>

        .stat-label {

    <!-- Hero Section -->            font-size: 1rem;

    <section class="hero">            opacity: 0.95;

        <div class="hero-content">            color: var(--text-secondary);

            <h1><span class="gradient-text">EverSpace</span></h1>        }

            <p class="subtitle">Where Conversations Come Alive</p>

            <p style="font-size: 1.2rem; color: #8b949e; margin-bottom: 3rem; max-width: 900px; margin-left: auto; margin-right: auto;">        /* Testimonials Section */

                Experience the next generation of secure, real-time communication. With military-grade security, lightning-fast messaging, and stunning visual design, EverSpace transforms how you connect with others. Join a community that values privacy, speed, and beautiful design.        .testimonials {

            </p>            padding: 8rem 2rem;

            <div class="cta-buttons">            background: linear-gradient(135deg, var(--dark-card) 0%, var(--darker-card) 100%);

                {% if user.is_authenticated %}        }

                    <a href="{% url 'chat:index' %}" class="btn btn-primary">

                        <i class="fas fa-rocket"></i> Launch Into Chat        .testimonials-container {

                    </a>            max-width: 1200px;

                {% else %}            margin: 0 auto;

                    <a href="{% url 'authentication:register' %}" class="btn btn-primary">        }

                        <i class="fas fa-rocket"></i> Launch Into Chat

                    </a>        .section-title {

                {% endif %}            text-align: center;

                <a href="#features" class="btn btn-secondary">            font-size: 3rem;

                    <i class="fas fa-compass"></i> Explore Features            font-weight: 700;

                </a>            margin-bottom: 4rem;

            </div>            background: var(--warning-gradient);

        </div>            -webkit-background-clip: text;

    </section>            -webkit-text-fill-color: transparent;

            background-clip: text;

    <!-- Features Section -->        }

    <section class="features" id="features">

        <h2 class="section-title">Revolutionary Features</h2>        .testimonials-grid {

        <div class="features-grid">            display: grid;

            <div class="feature-card">            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));

                <div class="feature-icon">            gap: 2rem;

                    <i class="fas fa-shield-halved"></i>        }

                </div>

                <h3>Military-Grade Security</h3>        .testimonial-card {

                <p>Multi-factor authentication, end-to-end encryption, and advanced threat protection keep your conversations absolutely secure.</p>            background: var(--dark-card);

            </div>            border: 1px solid var(--glass-border);

            border-radius: var(--border-radius-lg);

            <div class="feature-card">            padding: 2rem;

                <div class="feature-icon">            transition: all 0.3s ease;

                    <i class="fas fa-bolt"></i>            box-shadow: var(--shadow-glass);

                </div>        }

                <h3>Lightning Fast</h3>

                <p>Real-time messaging with WebSocket technology ensures your messages arrive instantly with zero lag.</p>        .testimonial-card:hover {

            </div>            transform: translateY(-5px);

            box-shadow: var(--shadow-elevated);

            <div class="feature-card">            border-color: var(--accent-mint);

                <div class="feature-icon">        }

                    <i class="fas fa-sparkles"></i>

                </div>        .testimonial-content {

                <h3>Immersive Experience</h3>            margin-bottom: 1.5rem;

                <p>Beautiful animations, smooth transitions, and a modern interface that makes chatting a delightful experience.</p>        }

            </div>

        .testimonial-content p {

            <div class="feature-card">            font-size: 1.1rem;

                <div class="feature-icon">            line-height: 1.6;

                    <i class="fas fa-layer-group"></i>            color: var(--text-primary);

                </div>            font-style: italic;

                <h3>Multiple Rooms</h3>        }

                <p>Create or join unlimited chat rooms. Organize conversations by topics, teams, or interests.</p>

            </div>        .testimonial-author {

            display: flex;

            <div class="feature-card">            align-items: center;

                <div class="feature-icon">            gap: 1rem;

                    <i class="fas fa-mobile-screen"></i>        }

                </div>

                <h3>Cross-Platform</h3>        .author-avatar {

                <p>Fully responsive design works seamlessly on desktop, tablet, and mobile devices. Chat anywhere, anytime.</p>            width: 50px;

            </div>            height: 50px;

            background: var(--primary-gradient);

            <div class="feature-card">            border-radius: 50%;

                <div class="feature-icon">            display: flex;

                    <i class="fas fa-code"></i>            align-items: center;

                </div>            justify-content: center;

                <h3>Modern Technology</h3>            color: var(--text-primary);

                <p>Built with cutting-edge web technologies including Django Channels, WebSockets, and modern JavaScript.</p>            font-size: 1.2rem;

            </div>        }

        </div>

    </section>        .author-name {

            font-weight: 600;

    <!-- Stats Section -->            color: var(--text-primary);

    <section class="stats" id="stats">            margin-bottom: 0.25rem;

        <div class="stats-grid">        }

            <div class="stat-item">

                <div class="stat-number">100<small style="font-size: 0.6em;">%</small></div>        .author-role {

                <div class="stat-label">Uptime</div>            color: var(--text-secondary);

            </div>            font-size: 0.9rem;

            <div class="stat-item">        }

                <div class="stat-number">256</div>

                <div class="stat-label">Bit Encryption</div>        /* Call to Action Section */

            </div>        .cta-section {

            <div class="stat-item">            padding: 8rem 2rem;

                <div class="stat-number">0.001<small style="font-size: 0.6em;">s</small></div>            text-align: center;

                <div class="stat-label">Second Latency</div>            background: linear-gradient(135deg, var(--darker-card) 0%, var(--dark-bg) 100%);

            </div>        }

            <div class="stat-item">

                <div class="stat-number">24<small style="font-size: 0.6em;">/7</small></div>        .cta-container {

                <div class="stat-label">Support</div>            max-width: 800px;

            </div>            margin: 0 auto;

        </div>        }

    </section>

        .cta-title {

    <!-- Testimonials Section -->            font-size: 3rem;

    <section class="testimonials" id="testimonials">            font-weight: 700;

        <h2 class="section-title">What Our Users Say</h2>            margin-bottom: 1.5rem;

        <div class="testimonials-grid">            background: var(--primary-gradient);

            <div class="testimonial-card">            -webkit-background-clip: text;

                <p class="testimonial-text">"EverSpace has revolutionized how our team communicates. The security features give us peace of mind, and the interface is absolutely beautiful!"</p>            -webkit-text-fill-color: transparent;

                <div class="testimonial-author">            background-clip: text;

                    <div class="author-avatar">SJ</div>        }

                    <div class="author-info">

                        <h4>Sarah Johnson</h4>        .cta-description {

                        <p>Tech Lead, InnovateCorp</p>            font-size: 1.2rem;

                    </div>            color: var(--text-secondary);

                </div>            margin-bottom: 3rem;

            </div>            line-height: 1.6;

        }

            <div class="testimonial-card">

                <p class="testimonial-text">"The real-time messaging is incredibly fast, and I love the modern design. It makes chatting feel like a premium experience."</p>        .cta-buttons {

                <div class="testimonial-author">            display: flex;

                    <div class="author-avatar">AC</div>            gap: 1rem;

                    <div class="author-info">            justify-content: center;

                        <h4>Alex Chen</h4>            flex-wrap: wrap;

                        <p>Software Engineer</p>            margin-bottom: 3rem;

                    </div>        }

                </div>

            </div>        .btn-cta-primary {

            background: var(--primary-gradient);

            <div class="testimonial-card">            color: var(--text-primary);

                <p class="testimonial-text">"Multi-factor authentication and room management features are exactly what we needed for our organization. Highly recommended!"</p>            padding: 1rem 2rem;

                <div class="testimonial-author">            font-size: 1.1rem;

                    <div class="author-avatar">MP</div>            border-radius: 50px;

                    <div class="author-info">            border: none;

                        <h4>Michael Park</h4>            box-shadow: 0 8px 30px rgba(255, 107, 107, 0.4);

                        <p>Operations Manager</p>            transition: all 0.3s ease;

                    </div>        }

                </div>

            </div>        .btn-cta-primary:hover {

        </div>            transform: translateY(-3px);

    </section>            box-shadow: 0 12px 40px rgba(255, 107, 107, 0.6);

            filter: brightness(1.1);

    <!-- Footer -->        }

    <footer>

        <div class="footer-content">        .btn-cta-secondary {

            <div class="footer-logo gradient-text">🚀 EverSpace</div>            background: var(--dark-card);

            <div class="footer-links">            color: var(--accent-teal);

                <a href="#features">Features</a>            padding: 1rem 2rem;

                <a href="#stats">Stats</a>            font-size: 1.1rem;

                <a href="#testimonials">Testimonials</a>            border-radius: 50px;

                {% if not user.is_authenticated %}            border: 2px solid var(--accent-teal);

                    <a href="{% url 'authentication:login' %}">Login</a>            transition: all 0.3s ease;

                    <a href="{% url 'authentication:register' %}">Register</a>        }

                {% endif %}

            </div>        .btn-cta-secondary:hover {

            <p class="copyright">© 2025 EverSpace. All rights reserved.</p>            background: var(--accent-teal);

        </div>            color: var(--dark-bg);

    </footer>            box-shadow: 0 8px 30px rgba(78, 205, 196, 0.4);

</body>        }

</html>

        .cta-features {
            display: flex;
            gap: 2rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .cta-feature {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--text-secondary);
        }

        .cta-feature i {
            color: var(--accent-mint);
        }

        /* Ultra Modern Footer */
        .footer {
            position: relative;
            background: linear-gradient(135deg, var(--dark-bg) 0%, var(--darker-card) 50%, var(--dark-card) 100%);
            overflow: hidden;
        }

        .footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 1px;
            background: var(--primary-gradient);
            animation: footerGlow 3s ease-in-out infinite alternate;
        }

        @keyframes footerGlow {
            0% { box-shadow: 0 0 10px rgba(255, 107, 107, 0.3); }
            100% { box-shadow: 0 0 30px rgba(255, 107, 107, 0.8), 0 0 60px rgba(78, 205, 196, 0.4); }
        }

        .footer-waves {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100px;
            background: linear-gradient(135deg, rgba(255, 107, 107, 0.1), rgba(78, 205, 196, 0.1));
            clip-path: polygon(0 0, 100% 0, 100% 70%, 0 100%);
        }

        .footer-content {
            position: relative;
            max-width: 1200px;
            margin: 0 auto;
            padding: 5rem 2rem 3rem;
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 4rem;
            z-index: 2;
        }

        .footer-brand {
            max-width: 350px;
            position: relative;
        }

        .footer-logo {
            font-size: 2rem;
            font-weight: 800;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .footer-logo i {
            color: var(--accent-coral);
            filter: drop-shadow(0 0 10px rgba(255, 107, 107, 0.5));
            animation: rocketFloat 4s ease-in-out infinite;
        }

        @keyframes rocketFloat {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            50% { transform: translateY(-5px) rotate(5deg); }
        }

        .footer-description {
            color: var(--text-secondary);
            line-height: 1.6;
            margin-bottom: 2rem;
            font-size: 1.1rem;
        }

        .footer-social {
            display: flex;
            gap: 1rem;
            margin-bottom: 2rem;
        }

        .social-link {
            width: 50px;
            height: 50px;
            background: var(--dark-card);
            border: 1px solid var(--glass-border);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }

        .social-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--primary-gradient);
            opacity: 0;
            transition: opacity 0.4s ease;
            z-index: -1;
        }

        .social-link:hover::before {
            opacity: 1;
        }

        .social-link:hover {
            color: var(--text-primary);
            transform: translateY(-5px) scale(1.1);
            box-shadow: 0 10px 25px rgba(255, 107, 107, 0.3);
            border-color: transparent;
        }

        .footer-links {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 3rem;
        }

        .footer-section {
            position: relative;
        }

        .footer-section::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 3px;
            height: 30px;
            background: var(--secondary-gradient);
            border-radius: 2px;
            opacity: 0.7;
        }

        .footer-section h4 {
            color: var(--text-primary);
            margin-bottom: 1.5rem;
            font-weight: 700;
            font-size: 1.1rem;
            padding-left: 1rem;
            position: relative;
        }

        .footer-section ul {
            list-style: none;
            padding-left: 1rem;
        }

        .footer-section ul li {
            margin-bottom: 0.75rem;
            position: relative;
        }

        .footer-section ul li a {
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.25rem 0;
            position: relative;
        }

        .footer-section ul li a::before {
            content: '';
            width: 0;
            height: 2px;
            background: var(--accent-teal);
            position: absolute;
            bottom: 0;
            left: 0;
            transition: width 0.3s ease;
        }

        .footer-section ul li a:hover::before {
            width: 100%;
        }

        .footer-section ul li a:hover {
            color: var(--accent-teal);
            transform: translateX(5px);
        }

        .footer-newsletter {
            margin-top: 2rem;
            padding: 1.5rem;
            background: var(--darker-card);
            border: 1px solid var(--glass-border);
            border-radius: var(--border-radius-lg);
            backdrop-filter: blur(10px);
        }

        .newsletter-title {
            color: var(--text-primary);
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .newsletter-description {
            color: var(--text-secondary);
            font-size: 0.9rem;
            margin-bottom: 1rem;
        }

        .newsletter-form {
            display: flex;
            gap: 0.5rem;
        }

        .newsletter-input {
            flex: 1;
            background: var(--dark-bg);
            border: 1px solid var(--glass-border);
            border-radius: var(--border-radius-sm);
            padding: 0.75rem;
            color: var(--text-primary);
            font-size: 0.9rem;
            transition: all 0.3s ease;
        }

        .newsletter-input:focus {
            outline: none;
            border-color: var(--accent-teal);
            box-shadow: 0 0 0 2px rgba(78, 205, 196, 0.2);
        }

        .newsletter-input::placeholder {
            color: var(--text-muted);
        }

        .newsletter-btn {
            background: var(--secondary-gradient);
            border: none;
            border-radius: var(--border-radius-sm);
            padding: 0.75rem 1rem;
            color: var(--text-primary);
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .newsletter-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(150, 206, 180, 0.4);
        }

        .footer-bottom {
            position: relative;
            background: var(--dark-bg);
            border-top: 1px solid var(--glass-border);
            padding: 2.5rem 2rem;
            text-align: center;
        }

        .footer-bottom-content {
            max-width: 1200px;
            margin: 0 auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .footer-copyright {
            color: var(--text-muted);
            font-size: 0.9rem;
        }

        .footer-badges {
            display: flex;
            gap: 1rem;
            align-items: center;
        }

        .footer-badge {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1rem;
            background: var(--dark-card);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            color: var(--text-secondary);
            font-size: 0.8rem;
            transition: all 0.3s ease;
        }

        .footer-badge:hover {
            border-color: var(--accent-mint);
            color: var(--accent-mint);
            transform: translateY(-2px);
        }

        .footer-badge i {
            font-size: 0.9rem;
        }

        .footer-particles {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: 1;
        }

        .footer-particle {
            position: absolute;
            width: 2px;
            height: 2px;
            background: linear-gradient(45deg, var(--accent-coral), var(--accent-teal));
            border-radius: 50%;
            animation: footerFloat 8s infinite ease-in-out;
            opacity: 0.3;
        }

        .footer-particle {
            position: absolute;
            background: linear-gradient(45deg, var(--coral), var(--teal));
            border-radius: 50%;
            opacity: 0.6;
            animation: footerFloat linear infinite;
            pointer-events: none;
        }

        @keyframes footerFloat {
            0%, 100% { transform: translateY(0px) rotate(0deg); opacity: 0.3; }
            50% { transform: translateY(-20px) rotate(180deg); opacity: 0.7; }
        }

        /* Mobile Responsiveness */
        @media (max-width: 768px) {
            .nav {
                padding: 0 1rem;
            }
            
            .hero-title {
                font-size: 3rem;
            }
            
            .hero-cta {
                flex-direction: column;
                align-items: center;
            }
            
            .btn-hero {
                width: 100%;
                max-width: 300px;
            }
            
            .features {
                padding: 4rem 1rem;
            }
            
            .features-grid {
                grid-template-columns: 1fr;
            }

            .testimonials-grid {
                grid-template-columns: 1fr;
            }

            .cta-title {
                font-size: 2rem;
            }

            .cta-buttons {
                flex-direction: column;
                align-items: center;
            }

            .btn-cta-primary,
            .btn-cta-secondary {
                width: 100%;
                max-width: 300px;
            }

            .cta-features {
                flex-direction: column;
                gap: 1rem;
            }

            .footer-content {
                grid-template-columns: 1fr;
                gap: 3rem;
                padding: 3rem 1rem 2rem;
            }

            .footer-links {
                grid-template-columns: 1fr;
                gap: 2rem;
            }

            .footer-social {
                justify-content: center;
            }

            .newsletter-form {
                flex-direction: column;
            }

            .footer-bottom-content {
                flex-direction: column;
                text-align: center;
                gap: 1.5rem;
            }

            .footer-badges {
                flex-wrap: wrap;
                justify-content: center;
            }
        }

        /* Scroll animations */
        .scroll-fade {
            opacity: 0;
            transform: translateY(30px);
            transition: all 0.6s ease;
        }

        .scroll-fade.visible {
            opacity: 1;
            transform: translateY(0);
        }
    </style>
</head>
<body>
    <!-- Animated Background -->
    <div class="bg-animation"></div>
    
    <!-- Floating Particles -->
    <div class="particles" id="particles"></div>

    <!-- Header -->
    <header class="header">
        <nav class="nav">
            <div class="logo">
                <i class="fas fa-rocket"></i> EverSpace
            </div>
            <div class="nav-buttons">
                <a href="{% url 'auth_login' %}" class="btn btn-secondary">
                    <i class="fas fa-sign-in-alt"></i> Sign In
                </a>
                <a href="{% url 'auth_register' %}" class="btn btn-primary">
                    <i class="fas fa-user-plus"></i> Get Started
                </a>
            </div>
        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero">
        <div class="hero-content">
            <h1 class="hero-title">EverSpace</h1>
            <p class="hero-subtitle">Where Conversations Come Alive</p>
            <p class="hero-description">
                Experience the next generation of secure, real-time communication. With military-grade security, 
                lightning-fast messaging, and stunning visual design, EverSpace transforms how you connect with others. 
                Join a community that values privacy, speed, and beautiful design.
            </p>
            <div class="hero-cta">
                <a href="{% url 'auth_register' %}" class="btn btn-hero btn-hero-primary">
                    <i class="fas fa-rocket"></i> Launch Into Chat
                </a>
                <a href="#features" class="btn btn-hero btn-hero-secondary">
                    <i class="fas fa-info-circle"></i> Explore Features
                </a>
            </div>
        </div>
    </section>

    <!-- Features Section -->
    <section class="features scroll-fade" id="features">
        <h2 class="features-title">Revolutionary Features</h2>
        <div class="features-grid">
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <h3 class="feature-title">Military-Grade Security</h3>
                <p class="feature-description">
                    Multi-factor authentication, end-to-end encryption, and advanced threat protection 
                    keep your conversations absolutely secure.
                </p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-bolt"></i>
                </div>
                <h3 class="feature-title">Lightning Fast</h3>
                <p class="feature-description">
                    Real-time messaging with WebSocket technology ensures your messages arrive 
                    instantly with zero lag.
                </p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-magic"></i>
                </div>
                <h3 class="feature-title">Immersive Experience</h3>
                <p class="feature-description">
                    Beautiful animations, smooth transitions, and a modern interface that makes 
                    chatting a delightful experience.
                </p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-users"></i>
                </div>
                <h3 class="feature-title">Multiple Rooms</h3>
                <p class="feature-description">
                    Create or join unlimited chat rooms. Organize conversations by topics, 
                    teams, or interests.
                </p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-mobile-alt"></i>
                </div>
                <h3 class="feature-title">Cross-Platform</h3>
                <p class="feature-description">
                    Fully responsive design works seamlessly on desktop, tablet, and mobile devices. 
                    Chat anywhere, anytime.
                </p>
            </div>
            
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-rocket"></i>
                </div>
                <h3 class="feature-title">Modern Technology</h3>
                <p class="feature-description">
                    Built with cutting-edge web technologies including Django Channels, WebSockets, 
                    and modern JavaScript.
                </p>
            </div>
        </div>
    </section>

    <!-- Stats Section -->
    <section class="stats scroll-fade">
        <div class="stats-grid">
            <div class="stat-item">
                <div class="stat-number" data-target="99.9">0</div>
                <div class="stat-label">% Uptime</div>
            </div>
            <div class="stat-item">
                <div class="stat-number" data-target="256">0</div>
                <div class="stat-label">Bit Encryption</div>
            </div>
            <div class="stat-item">
                <div class="stat-number" data-target="0.001">0</div>
                <div class="stat-label">Second Latency</div>
            </div>
            <div class="stat-item">
                <div class="stat-number" data-target="24">0</div>
                <div class="stat-label">/ 7 Support</div>
            </div>
        </div>
    </section>

    <!-- Testimonials Section -->
    <section class="testimonials scroll-fade">
        <div class="testimonials-container">
            <h2 class="section-title">What Our Users Say</h2>
            <div class="testimonials-grid">
                <div class="testimonial-card">
                    <div class="testimonial-content">
                        <p>"EverSpace has revolutionized how our team communicates. The security features give us peace of mind, and the interface is absolutely beautiful!"</p>
                    </div>
                    <div class="testimonial-author">
                        <div class="author-avatar">
                            <i class="fas fa-user-tie"></i>
                        </div>
                        <div class="author-info">
                            <div class="author-name">Sarah Johnson</div>
                            <div class="author-role">Tech Lead, InnovateCorp</div>
                        </div>
                    </div>
                </div>

                <div class="testimonial-card">
                    <div class="testimonial-content">
                        <p>"The real-time messaging is incredibly fast, and I love the modern design. It makes chatting feel like a premium experience."</p>
                    </div>
                    <div class="testimonial-author">
                        <div class="author-avatar">
                            <i class="fas fa-user-graduate"></i>
                        </div>
                        <div class="author-info">
                            <div class="author-name">Alex Chen</div>
                            <div class="author-role">Software Engineer</div>
                        </div>
                    </div>
                </div>

                <div class="testimonial-card">
                    <div class="testimonial-content">
                        <p>"Multi-factor authentication and room management features are exactly what we needed for our organization. Highly recommended!"</p>
                    </div>
                    <div class="testimonial-author">
                        <div class="author-avatar">
                            <i class="fas fa-user-shield"></i>
                        </div>
                        <div class="author-info">
                            <div class="author-name">Maria Rodriguez</div>
                            <div class="author-role">Security Manager</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Call to Action Section -->
    <section class="cta-section scroll-fade">
        <div class="cta-container">
            <h2 class="cta-title">Ready to Transform Your Communication?</h2>
            <p class="cta-description">
                Join thousands of users who have already discovered the future of chat. 
                Get started today and experience the difference.
            </p>
            <div class="cta-buttons">
                <a href="{% url 'auth_register' %}" class="btn btn-cta-primary">
                    <i class="fas fa-rocket"></i> Start Chatting Now
                </a>
                <a href="{% url 'auth_login' %}" class="btn btn-cta-secondary">
                    <i class="fas fa-sign-in-alt"></i> Sign In
                </a>
            </div>
            <div class="cta-features">
                <div class="cta-feature">
                    <i class="fas fa-check-circle"></i>
                    <span>Free to get started</span>
                </div>
                <div class="cta-feature">
                    <i class="fas fa-check-circle"></i>
                    <span>No credit card required</span>
                </div>
                <div class="cta-feature">
                    <i class="fas fa-check-circle"></i>
                    <span>Setup in under 2 minutes</span>
                </div>
            </div>
        </div>
    </section>

    <!-- Ultra Modern Footer -->
    <footer class="footer">
        <div class="footer-waves"></div>
        <div class="footer-particles" id="footer-particles"></div>
        
        <div class="footer-content">
            <div class="footer-brand">
                <div class="footer-logo">
                    <i class="fas fa-rocket"></i> EverSpace
                </div>
                <p class="footer-description">
                    Revolutionizing communication with cutting-edge technology, military-grade security, 
                    and stunning design. Join the future of chat today.
                </p>
                
                <div class="footer-social">
                    <a href="#" class="social-link" title="GitHub">
                        <i class="fab fa-github"></i>
                    </a>
                    <a href="#" class="social-link" title="Discord">
                        <i class="fab fa-discord"></i>
                    </a>
                    <a href="#" class="social-link" title="Twitter">
                        <i class="fab fa-twitter"></i>
                    </a>
                    <a href="#" class="social-link" title="LinkedIn">
                        <i class="fab fa-linkedin"></i>
                    </a>
                </div>

                <div class="footer-newsletter">
                    <h4 class="newsletter-title">Stay Updated</h4>
                    <p class="newsletter-description">Get the latest updates and security insights</p>
                    <div class="newsletter-form">
                        <input type="email" class="newsletter-input" placeholder="your@email.com">
                        <button class="newsletter-btn">
                            <i class="fas fa-paper-plane"></i>
                        </button>
                    </div>
                </div>
            </div>
            
            <div class="footer-links">
                <div class="footer-section">
                    <h4><i class="fas fa-cube"></i> Product</h4>
                    <ul>
                        <li><a href="#features">
                            <i class="fas fa-star"></i> Features
                        </a></li>
                        <li><a href="{% url 'auth_register' %}">
                            <i class="fas fa-rocket"></i> Get Started
                        </a></li>
                        <li><a href="{% url 'auth_login' %}">
                            <i class="fas fa-sign-in-alt"></i> Sign In
                        </a></li>
                        <li><a href="#">
                            <i class="fas fa-mobile-alt"></i> Mobile App
                        </a></li>
                        <li><a href="#">
                            <i class="fas fa-desktop"></i> Desktop App
                        </a></li>
                    </ul>
                </div>
                
                <div class="footer-section">
                    <h4><i class="fas fa-shield-alt"></i> Security</h4>
                    <ul>
                        <li><a href="#">
                            <i class="fas fa-key"></i> Multi-Factor Auth
                        </a></li>
                        <li><a href="#">
                            <i class="fas fa-lock"></i> End-to-End Encryption
                        </a></li>
                        <li><a href="#">
                            <i class="fas fa-user-shield"></i> Privacy Policy
                        </a></li>
                        <li><a href="#">
                            <i class="fas fa-certificate"></i> Security Audit
                        </a></li>
                        <li><a href="#">
                            <i class="fas fa-bug"></i> Report Vulnerability
                        </a></li>
                    </ul>
                </div>
                
                <div class="footer-section">
                    <h4><i class="fas fa-headset"></i> Support</h4>
                    <ul>
                        <li><a href="#">
                            <i class="fas fa-question-circle"></i> Help Center
                        </a></li>
                        <li><a href="#">
                            <i class="fas fa-envelope"></i> Contact Us
                        </a></li>
                        <li><a href="#">
                            <i class="fas fa-chart-line"></i> System Status
                        </a></li>
                        <li><a href="#">
                            <i class="fas fa-book"></i> Documentation
                        </a></li>
                        <li><a href="#">
                            <i class="fas fa-video"></i> Video Tutorials
                        </a></li>
                    </ul>
                </div>
            </div>
        </div>
        
        <div class="footer-bottom">
            <div class="footer-bottom-content">
                <div class="footer-copyright">
                    &copy; 2025 EverSpace. Built with ❤️ and cutting-edge technology.
                </div>
                <div class="footer-badges">
                    <div class="footer-badge">
                        <i class="fas fa-shield-check"></i>
                        <span>SOC 2 Compliant</span>
                    </div>
                    <div class="footer-badge">
                        <i class="fas fa-award"></i>  
                        <span>ISO 27001</span>
                    </div>
                    <div class="footer-badge">
                        <i class="fas fa-globe"></i>
                        <span>GDPR Ready</span>
                    </div>
                </div>
            </div>
        </div>
    </footer>

    <script>
        // Create floating particles
        function createParticles() {
            const particles = document.getElementById('particles');
            const particleCount = 50;

            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.classList.add('particle');
                
                // Random size and position
                const size = Math.random() * 4 + 1;
                particle.style.width = size + 'px';
                particle.style.height = size + 'px';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 20 + 's';
                particle.style.animationDuration = (Math.random() * 10 + 10) + 's';
                
                particles.appendChild(particle);
            }
        }

        // Smooth scrolling for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Scroll animations
        function handleScrollAnimations() {
            const elements = document.querySelectorAll('.scroll-fade');
            const windowHeight = window.innerHeight;

            elements.forEach(element => {
                const elementTop = element.getBoundingClientRect().top;
                
                if (elementTop < windowHeight - 100) {
                    element.classList.add('visible');
                }
            });
        }

        // Animate numbers
        function animateNumbers() {
            const statNumbers = document.querySelectorAll('.stat-number');
            
            statNumbers.forEach(stat => {
                const target = parseFloat(stat.getAttribute('data-target'));
                const increment = target / 100;
                let current = 0;
                
                const timer = setInterval(() => {
                    current += increment;
                    if (current >= target) {
                        current = target;
                        clearInterval(timer);
                    }
                    stat.textContent = current.toFixed(current < 1 ? 3 : current < 10 ? 1 : 0);
                }, 20);
            });
        }

        // Header scroll effect
        function handleHeaderScroll() {
            const header = document.querySelector('.header');
            const scrollY = window.scrollY;
            
            if (scrollY > 100) {
                header.style.background = 'rgba(26, 32, 44, 0.95)';
                header.style.boxShadow = '0 4px 20px rgba(0, 0, 0, 0.3)';
            } else {
                header.style.background = 'rgba(26, 32, 44, 0.9)';
                header.style.boxShadow = 'none';
            }
        }

        // Create footer particles
        function createFooterParticles() {
            const footerParticles = document.getElementById('footer-particles');
            const particleCount = 20;

            for (let i = 0; i < particleCount; i++) {
                const particle = document.createElement('div');
                particle.classList.add('footer-particle');
                
                // Random size and position
                const size = Math.random() * 3 + 1;
                particle.style.width = size + 'px';
                particle.style.height = size + 'px';
                particle.style.left = Math.random() * 100 + '%';
                particle.style.top = Math.random() * 100 + '%';
                particle.style.animationDelay = Math.random() * 8 + 's';
                particle.style.animationDuration = (Math.random() * 4 + 6) + 's';
                
                footerParticles.appendChild(particle);
            }
        }

        // Initialize everything
        document.addEventListener('DOMContentLoaded', function() {
            createParticles();
            createFooterParticles();
            handleScrollAnimations();
            
            // Animate numbers when stats section is visible
            const statsSection = document.querySelector('.stats');
            const observer = new IntersectionObserver((entries) => {
                if (entries[0].isIntersecting) {
                    animateNumbers();
                    observer.disconnect();
                }
            });
            observer.observe(statsSection);
        });

        // Event listeners
        window.addEventListener('scroll', () => {
            handleScrollAnimations();
            handleHeaderScroll();
        });

        // Cursor trail effect (optional)
        document.addEventListener('mousemove', (e) => {
            const trail = document.createElement('div');
            trail.style.position = 'fixed';
            trail.style.left = e.clientX + 'px';
            trail.style.top = e.clientY + 'px';
            trail.style.width = '4px';
            trail.style.height = '4px';
            trail.style.background = 'rgba(102, 126, 234, 0.6)';
            trail.style.borderRadius = '50%';
            trail.style.pointerEvents = 'none';
            trail.style.zIndex = '9999';
            trail.style.animation = 'trailFade 0.5s ease-out forwards';
            
            document.body.appendChild(trail);
            
            setTimeout(() => trail.remove(), 500);
        });

        // Add trail fade animation
        const style = document.createElement('style');
        style.textContent = `
            @keyframes trailFade {
                0% { opacity: 1; transform: scale(1); }
                100% { opacity: 0; transform: scale(0); }
            }
        `;
        document.head.appendChild(style);
    </script>
</body>
</html>